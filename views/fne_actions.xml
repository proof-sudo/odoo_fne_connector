<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Action serveur envoi groupé -->
        <record id="action_server_send_to_fne" model="ir.actions.server">
            <field name="name">Envoyer à la DGI</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">
if records:
    records.action_send_to_fne()
            </field>
        </record>
    </data>
</odoo>

